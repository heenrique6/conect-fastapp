specVersion: "v1"
serviceId: "conectfast"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "conectfast-us"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
